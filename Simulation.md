this document is used to learn and simulate codes generated by artificial intelligence.

## MoudubleBase.h ##

使用C语言模拟面向对象（继承，多态）

#include <stdint.h>
#include <stddef.h> (包含常用定义宏,最重要的是包含NULL)

虚函数表的构建
typedef struct{
    int (*init)(void *self);
    int (*run)(void *self);
    int (*cleanup)(void *self);
    void (*reset)(void *self);
}ModuleVTable